name: Build

on:
  push:
    branches:
      - '**'
    paths-ignore:
      - '**/README.md'
      - '.github/**'
  workflow_dispatch:

defaults:
  run:
    shell: pwsh

jobs:
  build:
    name: Build artifacts
    uses: ./.github/workflows/build_artifacts.yml
    with:
      isRelease: false